#include "main.h"

/**
 * main - prints _putchar, followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a[] = "_putchar";
	int b;

	for (b = 0; b < 0; b++)
	{
		_putchar(a[]);
	}
	_putchar('\n');

return (0);
}
